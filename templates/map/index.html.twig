{% extends 'base.html.twig' %}

{% block body %}
	<h1 class="text-3xl font-bold mt-3 mb-3 text-center">Carte des lieux d'intérêt</h1>

	<div class="map-container">
		<div class="map-layers">
			<div class="map-layers-button">
				<img class="map-layers-icon" src="{{ asset('icons/layers_map.png') }}" alt="Layers Map">
			</div>
		</div>
		<div class="map-center-button">
			<img class="map-center-icon" src="{{ asset('icons/center_map.png') }}" alt="Center Map">
		</div>

		<div data-controller="map geolocate" data-action="map:loaded->geolocate#onMapLoaded" data-map-categories-value="{{ categories|json_encode }}" data-map-token-value="{{ csrf_token }}" {% if position is defined %} data-map-position-value="{{ position|json_encode|default('') }}" {% endif %} style="height: 100%; width: 100%;"></div>

	</div>

	<div id="map-details">
		{% include 'map/_map_details_none.html.twig' %}
	</div>
{% endblock %}
