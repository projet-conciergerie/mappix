{% extends 'base.html.twig' %}

{% block body %}
	<div
		class="map-container" style="width: 100%; height: 400px;" {{ stimulus_controller('geolocate') }}>
		{# The map must have a defined height #}
		{# {{ ux_map(map, {style: 'height: 100%;'}) }} #}

		<div data-controller="map" style="height: 400px;"></div>

	</div>
	{% include 'map/_map_details_none.html.twig' %}
{% endblock %}
